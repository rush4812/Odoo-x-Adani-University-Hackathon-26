# Copy this file to .env and fill values
# Use a Gmail account with App Password (recommended) or other SMTP creds
EMAIL_USER=your.email@gmail.com
EMAIL_PASS=your_gmail_app_password
JWT_SECRET=replace_with_a_long_secret
PORT=5000

# If you want OTPs to always be delivered to a specific Gmail account (e.g. your test Gmail),
# set `OTP_TARGET_EMAIL` to that address. When set, OTPs will be sent to this address
# instead of the address entered in the forgot-password form. `replyTo` will remain
# the user-entered email so replies go to them.
OTP_TARGET_EMAIL=your.target.receiver@gmail.com

# Optionally change database path or other settings
